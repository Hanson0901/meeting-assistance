# 實時日誌功能指南

## 🆕 新功能概述

現已添加實時日誌查看功能，讓用戶可以在網頁上實時監控會議處理的詳細過程。

---

## 📊 功能組成

### 1. 訊息日誌面板（Messages Log）

位置：會議進度面板上方

**特點**：
- 顯示高級摘要信息（如 "✓ ASR 轉錄完成", "▶ ASR 已開始執行..."）
- 每條訊息前帶有不同的圖標：
  - ✓ 成功（綠色）
  - ✗ 錯誤（紅色）
  - ⚠ 警告（黃色）
  - ℹ️ 信息（藍色）
- **可點擊展開**：點擊任意訊息即可展開查看該操作的詳細系統日誌

### 2. 系統日誌面板（System Logs）

位置：訊息日誌面板下方

**特點**：
- 顯示所有系統日誌的詳細信息
- 實時更新（自動或手動）
- 日誌按類型自動著色：
  - 🔵 INFO（藍色）：普通信息
  - 🟢 SUCCESS（綠色）：成功操作
  - 🟡 WARNING（黃色）：警告
  - 🔴 ERROR（紅色）：錯誤
  - ⚫ STEP（深藍）：步驟開始
- 可自動滾動到最新日誌
- 可手動清空日誌

**控制按鈕**：
- 「清空」：清除所有日誌
- 「自動更新」勾選框：開啟/關閉自動更新

---

## 📝 日誌類型

### 系統記錄的事件

#### 會話創建
```
[HH:MM:SS] 會話已建立
```

#### 文件上傳
```
[HH:MM:SS] [WEB][session_id] 上傳音頻: /path/to/file.mkv
[HH:MM:SS] [WEB][session_id] 檔案大小: 150.25 MB
```

#### ASR（語音轉文字）
```
[HH:MM:SS] [STEP] 開始執行 ASR 語音轉文字...
[HH:MM:SS] [SUCCESS] ASR 轉錄完成
```

#### PKD（參與者/要點/決策提取）
```
[HH:MM:SS] [STEP] 開始執行 PKD 報告提取...
[HH:MM:SS] [SUCCESS] PKD 報告生成完成
```

#### Actions（行動項目）
```
[HH:MM:SS] [STEP] 開始提取行動項目...
[HH:MM:SS] [SUCCESS] 行動項目提取完成
```

#### Summary（摘要生成）
```
[HH:MM:SS] [STEP] 開始生成會議摘要...
[HH:MM:SS] [SUCCESS] 會議摘要生成完成
```

#### Export（TXT 匯出）
```
[HH:MM:SS] [STEP] 開始將結果匯出為 TXT 檔案...
[HH:MM:SS] [SUCCESS] TXT 匯出完成，共 3 個檔案
```

#### Bluetooth（藍牙傳送）
```
[HH:MM:SS] [STEP] 開始進行藍牙檔案傳送...
[HH:MM:SS] [STEP] 正在搜尋已配對的藍牙設備...
[HH:MM:SS] [SUCCESS] 藍牙傳送完成，目標設備: Device Name (MAC)
```

---

## 🎯 使用流程

### 1. 查看整體進度
直接查看訊息日誌面板，了解各步驟的完成狀態。

### 2. 查看詳細日誌
```
操作：
1. 在訊息日誌中找到要查看的消息
2. 點擊消息項（消息項會展開）
3. 在展開的部分查看該操作的詳細日誌
或
1. 使用下方的系統日誌面板查看所有日誌
2. 啟用「自動更新」勾選框實時更新日誌
```

### 3. 監控長時間運行的任務
- ASR 可能需要數分鐘
- 開啟「自動更新」，實時查看進度
- 日誌會自動更新最新 100 條記錄

### 4. 故障排查
- 查看錯誤日誌（紅色）
- 錯誤信息包含具體的失敗原因
- 根據錯誤提示採取相應行動

---

## 🔧 技術實現

### 後端（Python/Flask）

**日誌存儲**：
- 每個會話的日誌存儲在內存中
- 數據結構：`session_logs[session_id] = [log1, log2, ...]`
- 時間戳自動添加

**日誌 API**：
```
GET /api/session/<session_id>/logs
```

返回最後 100 條日誌（節省頻寬）：
```json
{
  "success": true,
  "logs": [
    "[HH:MM:SS] 日誌 1",
    "[HH:MM:SS] 日誌 2",
    ...
  ],
  "total": 150
}
```

**日誌函數**：
```python
def log_message(session_id, message):
    """同時記錄到標準輸出和內存"""
    print(message)
    timestamp = datetime.now().strftime("%H:%M:%S")
    log_entry = f"[{timestamp}] {message}"
    session_logs[session_id].append(log_entry)
```

### 前端（JavaScript）

**日誌更新函數**：
```javascript
async function updateSystemLogs()
```
- 定期從後端拉取最新日誌
- 更新系統日誌面板
- 同時更新展開的訊息項中的日誌

**訊息項展開/折疊**：
```javascript
function expandMessage(messageItem)
```
- 切換展開/折疊狀態
- 展開時自動加載系統日誌

**HTML 彩色日誌**：
- 根據日誌內容類型自動分類
- 應用不同的 CSS 類進行著色

---

## 📈 性能考慮

### 日誌存儲限制
- 每個會話最多保存所有日誌
- 只在前端顯示最後 100 條（節省頻寬）

### 更新頻率
- 默認狀態檢查間隔：5 秒
- 日誌自動更新間隔：同狀態檢查間隔
- 用戶可通過勾選框控制自動更新

### 優化建議
- 系統日誌面板默認隱藏（只顯示訊息）
- 只展開需要查看詳細日誌的訊息項
- 不需要時關閉「自動更新」

---

## 🐛 故障排除

### 日誌面板不更新

**原因**：
- 「自動更新」未勾選
- 網絡連接問題

**解決**：
```
1. 確認「自動更新」勾選框已選中
2. 手動點擊訊息項強制更新
3. 檢查瀏覽器控制台（F12）查看網絡錯誤
```

### 日誌丟失

**原因**：
- 會話已結束
- 頁面刷新

**說明**：
- 日誌存儲在內存中，會話結束後丟失
- 頁面刷新會丟失當前日誌視圖

**建議**：
- 在會話結束前，截圖或記錄重要信息
- 使用瀏覽器開發工具（F12）的 Console 標籤查看全部消息

---

## 📋 代碼示例

### 添加自定義日誌

在 web_app.py 中添加步驟時：
```python
log_message(session_id, f"[STEP] 開始執行新步驟...")
# 執行操作
log_message(session_id, f"[SUCCESS] 新步驟完成")
```

### 前端接收日誌

```javascript
// 自動每 5 秒更新一次（如果自動更新勾選）
if (document.getElementById('autoRefreshLogs').checked) {
    updateSystemLogs();
}
```

---

## ✨ 特色功能

| 功能 | 描述 | 優勢 |
|------|------|------|
| 實時更新 | 日誌實時反映系統狀態 | 用戶知道正在發生什麼 |
| 彩色分類 | 不同類型日誌不同顏色 | 快速識別問題 |
| 可點擊展開 | 訊息項可展開查看詳細日誌 | 簡潔與詳細的平衡 |
| 自動著色 | 根據內容自動判斷日誌類型 | 無需手動配置 |
| 時間戳 | 每條日誌都帶時間戳 | 追蹤事件順序 |
| 性能優化 | 只保存最後 100 條 | 節省內存和頻寬 |

---

## 🚀 最佳實踐

1. **監控長時間任務**：為 ASR 等耗時操作啟用自動更新
2. **排查問題**：出現錯誤時，查看詳細日誌找出原因
3. **驗證完成**：在訊息日誌中查看所有步驟是否標記為完成
4. **記錄重要信息**：在會話結束前，記下重要的日誌信息

---

## 🔄 版本歷史

| 版本 | 日期 | 變更 |
|------|------|------|
| 1.1.0 | 2024-08 | 添加實時日誌功能 |
| 1.0.1 | 2024-08 | 添加 ASR 驗證 |
| 1.0.0 | 2024-08 | 初始發布 |

---

## 📞 支持

如有問題，請：
1. 查看系統日誌尋找錯誤信息
2. 檢查瀏覽器控制台（F12）
3. 參考其他指南文檔

---

**更新日期**: 2024-08  
**狀態**: ✅ 已實施和測試  
